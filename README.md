vcpkg-CMake minimal working example
===================================

```
git submodule update --init --recursive --recommend-shallow --depth 1
cmake -B build -S .
cmake --build build
build/test1
```
